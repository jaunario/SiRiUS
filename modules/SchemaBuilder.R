# Title: Schema Builder
# Description: Schema is a setting of a simulation over an area 
# The Schema builder allows the user to define the a schema. This is where the area of interest and ORYZA parameters are set.
#

# 